void select_console_printer(){
  gEnv->SetValue("Print.Printer","ACC5585");
  std::cout << "Selected printer: ACC5585 at AVF Console Room" << std::endl;
  std::cout << "NB. This config is valid only in this settion." << std::endl;
  return;
}
